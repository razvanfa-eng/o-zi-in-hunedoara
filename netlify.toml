# Configurare Netlify.
# Site static în rădăcină + funcții în netlify/functions.

[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Pagina 404 personalizată
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
